pre-commit:
  parallel: true
  commands:
    typecheck:
      run: npm run typecheck
    lint-md:
      glob: "*.md"
      run: npx markdownlint {staged_files}
    test:
      run: npm test

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
