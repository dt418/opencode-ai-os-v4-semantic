pre-commit:
  parallel: true
  commands:
    typecheck:
      run: npm run typecheck
    lint-md:
      glob: "*.md"
      run: npx markdownlint {staged_files}
    test:
      run: npm test

# commit-msg hook disabled: no standard conventional-commit validator npm package
# commit-msg:
#   commands:
#     conventional:
#       run: npx lefthook-commit-msg-hook {commit_msg_file}
